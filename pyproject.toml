[project]
name = "repotutor"
requires-python = ">=3.11"
dependencies = [
    "textual>=7.0",
    "httpx>=0.24",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
exclude = ["tests/fixtures/"]
